# Add page

Follow the project `add-page` skill end-to-end:

1. types → mocks → api/client → page → App route → AppLayout nav (if needed)
2. No direct page imports of mocks; no `any`
3. Finish with `npm run lint && npm test && npm run build`
